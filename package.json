{
	"name": "nuxt-app",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "nuxt build",
		"dev": "nuxt dev --https --ssl-cert nexon.com.pem --ssl-key nexon.com-key.pem --port 443 --host supervive.nexon.com",
		"generate": "nuxt generate",
		"preview": "nuxt preview",
		"postinstall": "nuxt prepare"
	},
	"dependencies": {
		"@pinia/nuxt": "^0.11.2",
		"nuxt": "^4.2.0",
		"pinia": "^3.0.3",
		"vue": "latest"
	}
}
